{
  "name": "mern_login",
  "version": "1.0.0",
  "description": "Template for a MERN login",
  "main": "server.js",
  "scripts": {
    "client-install": "npm install --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=production node ./src/server/server.js",
    "server": "NODE_ENV=development nodemon ./src/server/server.js",
    "client": "npm start --prefix ./src/client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "concurrently": "^5.0.0",
    "express": "^4.17.1",
    "is-empty": "^1.2.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.8.0",
    "passport": "^0.4.1",
    "passport-jwt": "^4.0.0",
    "react-router-bootstrap": "^0.25.0",
    "validator": "^12.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
